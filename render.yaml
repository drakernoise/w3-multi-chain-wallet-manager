services:
  - type: web
    name: gravity-chat-server
    runtime: node
    region: frankfurt
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    rootDirectory: chat-server
    envVars:
      - key: NODE_VERSION
        value: 18.16.0
      - key: DB_PATH
        value: /opt/render/project/src/chat-server/
